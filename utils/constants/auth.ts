export enum AuthStatus {
  loading = "loading",
  signedIn = "signedIn",
  notSignedIn = "not-signedIn",
}

export const localStorageKeys = {};

export enum Roles {
  user = "user",
  superAdmin = "super-admin",
}
